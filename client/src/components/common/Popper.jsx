import React from 'react';

const Popper = ({ children }) => {
    return <div className="flex flex-col w-full min-h-[30px] bg-primary-100 rounded-lg p-2">{children}</div>;
};

export default Popper;
